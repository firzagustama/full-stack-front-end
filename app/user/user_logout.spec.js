'use strict';

describe('myApp.user.logout module', function() {

  beforeEach(module('myApp.user.logout'));

  describe('user logout controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var userLoginCtrl = $controller('UserLogoutCtrl');
      expect(userLoginCtrl).toBeDefined();
    }));

  });
});